# Backend Dependencies для AgentsPool
# Оптимизировано для CPU + Railway BuildKit cache

# === Core Framework ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# === Database ===
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# === Auth ===
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# === Web Scraping ===
requests>=2.31.0
beautifulsoup4>=4.12.0

# === Testing ===
pytest>=7.4.0
pytest-asyncio>=0.21.0

# === AI/ML (CPU-optimized) ===
# Указываем CPU-only PyTorch ПЕРЕД sentence-transformers
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers>=2.2.0

# numpy и scikit-learn УЖЕ включены в sentence-transformers
# Убрали дубликаты для ускорения установки

