# Railway configuration for AgentsPool
# This is a monorepo with backend and frontend services

[build]
builder = "NIXPACKS"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
